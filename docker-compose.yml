services:
  api:
    build: .
    container_name: pi-agent-harness
    env_file: .env
    ports:
      - "6006:6006"
    volumes:
      - ./:/workspace
    working_dir: /workspace
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "6006", "--reload"]
