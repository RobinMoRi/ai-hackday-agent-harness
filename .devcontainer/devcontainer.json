{
  "name": "ai-hackday-agent-harness",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--env-file", ".devcontainer/.env"],
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
  "remoteUser": "node",
  "mounts": [
    "source=pi-config,target=/home/node/.config,type=volume",
    "source=pi-home,target=/home/node/.pi,type=volume"
  ],
  "postCreateCommand": "sudo chown -R node:node /home/node/.config /home/node/.pi && mkdir -p /home/node/.pi/agent/extensions && cp /workspace/.devcontainer/extensions/package.json /home/node/.pi/agent/extensions/package.json && cp /workspace/.devcontainer/extensions/*.ts /home/node/.pi/agent/extensions/ && (cd /home/node/.pi/agent/extensions && npm install --omit=dev) && pi --version || true"
}
